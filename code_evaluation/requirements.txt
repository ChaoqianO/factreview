# ── Core workflow ──
langgraph>=0.2.0
pandas>=2.0.0
pyyaml>=6.0.0

# ── LLM providers (at least one needed for fix/judge with LLM) ──
openai>=1.0.0
anthropic>=0.45.0

# ── PDF extraction ──
pypdf>=4.0.0

# ── Reference checker (refchecker) ──
# These are required when --enable-refcheck is used.
# The package lazy-loads so the main workflow won't crash if they're absent.
requests>=2.25.0
beautifulsoup4>=4.9.0
arxiv>=1.4.0
tqdm>=4.60.0
colorama>=0.4.4
fuzzywuzzy>=0.18.0
python-Levenshtein>=0.12.0
bibtexparser>=1.4.0
pdfplumber>=0.6.0
numpy<2
python-dateutil>=2.8.0
